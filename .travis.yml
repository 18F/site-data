language: python

sudo: false

cache: pip

python:
  - "2.7"

before_script:
  - pip install -r requirements.txt
  - python manage.py db upgrade

script: nosetests -s

env:
- GITHUB_USER=test_user GITHUB_AUTH=sample_key
